# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

AS_ATD — an attendance system (ระบบเช็คชื่อ) for students in the Applied Statistics department. The user interface and domain vocabulary are in Thai; preserve Thai text verbatim in UI strings and database records.

## Status

The repository currently contains only `LICENSE` and `README.md` — no source code, build configuration, or tests exist yet. Architecture and tooling decisions are still open.

When scaffolding the project, capture the chosen stack (language, framework, package manager, test runner) and update this file with the actual build / lint / test commands and a real architecture overview. Do not invent commands until the tooling is in place.
